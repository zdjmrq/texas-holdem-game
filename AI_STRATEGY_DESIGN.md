# 德州扑克 AI 统一策略设计方案

> 状态：v1.4 已实施，等待最终体验确认  
> 目标版本：v1.4.0  
> 硬性要求：本地对局与联网房间中的 AI 必须使用同一个决策核心，只允许状态输入方式和执行方式不同。

## 1. 目标

新 AI 不追求完整 GTO 求解器，而采用适合桌面游戏实时运行的“信念驱动、风险约束、跨街规划、有限混合”策略。

它应当做到：

1. 合理比较行动的风险和收益，不因为随机性做明显亏损的动作。
2. 只使用自己的底牌、公共牌、公开行动、下注量、位置和筹码等合法信息。
3. 推测对手范围和行为倾向，并根据数据可信度控制调整幅度。
4. 从对手视角估计“自己的行动会被怎样理解”，形成反向博弈。
5. 一手牌内保持相对连贯的计划，同时允许新信息推翻旧计划。
6. 在价值相近的行动之间适度混合，让行为有变化但不失去逻辑。
7. 本地和联网 AI 在相同输入、相同随机种子下给出相同决策。
8. 能通过决策日志解释：为什么这样做、准备如何继续、什么情况会放弃。

## 2. 当前实现审计

### 2.1 值得保留的部分

- 已有多种 AI 风格参数，如入池率、激进度、诈唬频率和持续下注频率。
- 已有基础对手画像，记录 VPIP、PFR、翻后弃牌、跟注和加注等数据。
- 已有轻量对手范围宽度、牌面湿润度、听牌、阻断牌和 SPR 判断。
- 已能生成多个下注尺度并比较候选行动 EV。
- 已有持续下注、延迟持续下注、薄价值下注和过牌加注等局部策略。
- 已有合法动作修正层，可以阻止越界加注和非法行动。

### 2.2 需要重构的问题

1. 本地 AI 与服务端 AI 是两套实现。服务端策略更简单，联网体验与本地不一致。
2. 本地胜率模拟主要面对随机未知牌，对手范围没有真正进入发牌抽样。
3. 对手画像常被汇总成全桌平均值，无法准确区分主要对手。
4. 目前的随机变化多为固定概率开关，理由不够充分。
5. 同一个随机值可能连续控制多个判断，使不同策略事件出现非预期相关性。
6. 跨街计划只有少量过牌加注记忆，没有完整的价值、控池、半诈唬或放弃路线。
7. 没有“自身桌面形象”，AI 无法考虑对手眼中的自己。
8. 加注 EV 对多人跟注、被反加注、后续街权益实现率和反向隐含赔率估计较粗。
9. 性格中存在“为了像某种风格而犯错”的规则，应改成不同风险偏好下的合理选择。
10. 缺少完整的离线自对局评估体系，参数调整容易凭感觉。

## 3. 总体架构

```text
游戏公开状态
    ↓
State Adapter（本地/服务端各一个）
    ↓
统一 DecisionContext
    ↓
Opponent Belief + Self Image + Hand Plan
    ↓
范围胜率 / 对手回应 / 风险收益评估
    ↓
候选行动排序 + 有界混合
    ↓
Decision + DecisionTrace
    ↓
Action Adapter（本地/服务端各自执行）
```

建议拆分为以下纯逻辑模块：

```text
ai-core/
  policy.js              决策主入口
  context.js             标准化输入与合法行动
  rng.js                 可复现随机数
  profiles.js            AI 性格参数
  range-model.js         对手组合范围与动作更新
  player-model.js        对手行为后验
  self-image.js          对手眼中的 AI 形象
  equity.js              面向范围的蒙特卡洛胜率
  response-model.js      对手弃牌/跟注/反加注模型
  action-generator.js    候选行动和下注尺度
  utility.js             EV、风险、未来价值和欺骗价值
  hand-plan.js           跨街计划状态机
  mixer.js               有界混合策略
  trace.js               决策解释与调试信息
  variants/
    standard.js          标准德州规则参数
    short-deck.js        短牌规则参数
```

模块采用可同时被浏览器脚本和 CommonJS 调用的形式，不能依赖 DOM、WebSocket 或 Electron。游戏端只负责提供状态和执行返回的合法行动。

## 4. 统一输入与输出

### 4.1 DecisionContext

```js
{
  handId,
  decisionId,
  variant,                 // standard | shortdeck
  street,                  // preflop | flop | turn | river
  heroSeat,
  holeCards,
  communityCards,
  pot,
  currentBet,
  heroRoundBet,
  toCall,
  heroStack,
  effectiveStack,
  dealerSeat,
  activePlayers,
  legalActions,
  minRaiseTo,
  maxRaiseTo,
  publicActionHistory,
  showdownHistory,
  seed
}
```

必须由适配器保证：

- `pot` 的定义在两端完全相同。
- 加注金额统一表示为“本轮累计加注到多少”，而不是额外投入多少。
- 玩家座位和位置计算一致。
- 只传递 AI 合法可见的信息。
- 公共行动历史顺序稳定，重连后能重建同一画像。

### 4.2 Decision

```js
{
  action,                  // fold | check | call | raise | allin
  amount,
  planId,
  utility,
  confidence,
  trace
}
```

最终仍要经过服务端或本地规则引擎的合法性校验。策略层不能成为规则权威。

## 5. 对手信念模型

### 5.1 组合范围而不是单一“范围宽度”

每位对手维护一个 1326 组合（短牌使用相应牌库组合）的权重表。已知牌产生阻断后，非法组合权重归零。

初始范围由以下因素共同决定：

- 座位和相对按钮位置。
- 当前存活玩家数量。
- 有效筹码深度。
- 前面是否有人入池、加注或再加注。
- 对手长期 VPIP/PFR/3-bet 数据。

每次公开行动后更新：

```text
新权重(组合) ∝ 旧权重(组合) × P(该行动 | 该组合, 牌面, 位置, 下注量, 对手风格)
```

无需实现昂贵的精确贝叶斯求解，可以按牌力区间、听牌质量、阻断牌和摊牌价值使用分段似然。

### 5.2 行为画像与置信度

每个统计量使用带先验的后验均值，避免三五手牌就把玩家判断成极端类型。

需要按场景拆分：

- 翻牌前：开池、冷跟、3-bet、面对 3-bet 弃牌。
- 翻牌后：面对 1/3、1/2、3/4、满池以上下注的反应。
- 各街激进频率和河牌激进度。
- 单挑与多人底池分开统计。
- 是否在位置内。
- 摊牌时价值牌和诈唬的比例。

建议的置信度：

```text
confidence = observations / (observations + priorStrength)
最终估计 = populationPrior × (1-confidence) + observedRate × confidence
```

初期 `priorStrength` 可取 8～15 个有效观察，重要且稀有的河牌行为使用更强先验。

## 6. 面向范围的胜率与权益实现率

### 6.1 胜率

蒙特卡洛模拟从每位对手的加权组合范围中抽牌，而不是随机发两张牌。

返回值不只包含平均胜率：

```js
{
  equityMean,
  equityStdDev,
  nutShare,
  tieShare,
  sampleCount,
  confidence
}
```

### 6.2 权益实现率

同样的裸胜率在不同环境下价值不同，需要乘以权益实现率：

- 有位置、牌面容易继续、对手被动：实现率提高。
- 无位置、多人底池、边缘成牌、深筹码：实现率降低。
- 强听牌、坚果优势、低 SPR：实现率提高。
- 容易被更大成牌支配的听牌：加入反向隐含赔率惩罚。

```text
realizedEquity = rawEquity × realizationFactor
```

实现率应被限制在合理区间，例如 0.65～1.15，避免重复放大同一优势。

## 7. 候选行动和对手回应树

### 7.1 候选尺度

翻牌后不固定使用一组下注量，而是根据牌面和范围优势生成：

- 小尺度：25%～35% 底池，用于范围优势、干燥牌面和低成本施压。
- 中尺度：50%～70%，用于常规价值和半诈唬。
- 大尺度：80%～125%，用于极化、湿润牌面或坚果优势。
- 全下：仅在低 SPR、极化河牌或短筹码场景进入候选。

翻牌前根据位置、前方加注人数、有效筹码和是否在位置内生成开池、3-bet、4-bet 尺度。

### 7.2 对手回应

每个下注候选都估计每位对手的：

- 弃牌概率。
- 跟注概率。
- 反加注概率。
- 反加注后的范围强度。

估计因素包括：

- 对手组合范围中能继续的比例。
- 下注相对底池和筹码的大小。
- 牌面与范围/坚果优势。
- 对手长期行为。
- AI 当前线路是否可信。
- 对手眼中的 AI 形象。
- 多人底池中必须让所有对手弃牌的联合概率。

不应简单假设“下注越大，弃牌率一定越高”。面对强范围、跟注站或低 SPR 时，大尺度可能只是增加自身风险。

## 8. 风险收益函数

### 8.1 基础收益

跟注的基础净收益：

```text
EV(call) = equity × (pot + callCost) - callCost
```

加注需要按回应分支计算：

```text
EV(raise) =
  P(全部弃牌) × 当前底池
  + P(被跟注) × 被跟注后的权益价值
  + P(被反加注) × 最优后续回应价值
  - 当前投入
```

多人底池要区分无人继续、一人继续和多人继续，不能用单一平均弃牌率代替。

### 8.2 综合效用

```text
Utility(action) =
  chipEV
  + futureStreetValue
  + positionalValue
  + deceptionValue
  + informationValue
  - variancePenalty
  - ruinPenalty
  - reverseImpliedOdds
  - exploitabilityPenalty
```

约束：

- 风险项只在行动价值接近时起明显作用，不能让 AI 长期放弃清晰的正收益机会。
- 破产风险在筹码低于约 15BB 时显著提高，但进入合理推/弃阶段后重新以筹码 EV 为主。
- 多人底池、深筹码和低坚果潜力提高风险惩罚。
- 低 SPR、强范围优势和可靠价值牌降低风险惩罚。

## 9. 反向博弈：对手眼中的 AI

### 9.1 SelfImage

AI 维护一份只由公开行为构成的自身形象：

- 最近若干手 VPIP、PFR、3-bet、持续下注和河牌下注。
- 最近是否展示过诈唬。
- 最近展示的强牌是否采用过慢打。
- 当前桌面认为 AI 偏紧、偏松、偏激进还是偏被动。
- 当前行动路线的价值牌密度和诈唬密度。

这不是让 AI 假装知道玩家的具体想法，而是估计一个理性或半理性对手可能形成的印象。

### 9.2 线路可信度

对每个下注候选计算 `storyCredibility`：

- 当前行动是否与翻牌前范围一致。
- 前一街行动是否支持当前代表的强牌。
- 转牌/河牌是否更有利于 AI 的范围。
- AI 是否持有关键阻断牌。
- 当前尺度是否符合价值范围和诈唬范围的共同策略。

可信度影响对手回应模型，而不是直接强迫 AI 诈唬。

### 9.3 由反向博弈产生的变化

- 紧形象 + 对手过度弃牌：增加少量有阻断牌的诈唬。
- 诈唬刚被抓 + 对手开始放宽跟注：减少纯诈唬，扩大薄价值下注。
- 激进形象 + 强牌：增加诱导、较小尺度和过牌加注。
- 对手开始频繁反加注：减少无法继续的空气牌，增加强听牌和价值牌的可防守范围。
- 自己的线路无法合理代表强牌：即使静态弃牌率较高，也降低诈唬成功率预测。

## 10. 跨街 HandPlan

每手牌建立并持续更新：

```js
{
  id,
  intent,                 // value | thin_value | semi_bluff | bluff |
                          // bluff_catch | pot_control | trap
  targetSeats,
  maxRisk,
  preferredSizes,
  favorableRunouts,
  unfavorableRunouts,
  continueConditions,
  abortConditions,
  responseToRaise,
  confidence,
  history
}
```

示例：

```text
意图：半诈唬
依据：坚果同花听牌、有位置、对手翻牌弃牌偏高
当前动作：下注 55% 底池
继续牌：同花完成、高张压力牌、使对手范围封顶的配对牌
停止条件：空白转牌被跟注后，对手河牌范围明显增强
遭遇反加注：赔率满足时跟注，小 SPR 时考虑再推
```

计划必须可修正：新行动和新公共牌会重新计算，旧计划只作为连续性偏好，不能覆盖明显的新证据。

## 11. 有界混合策略

随机变化只允许发生在综合价值接近的候选之间。

建议规则：

1. 先排除非法行动和明显负收益行动。
2. 最佳行动与备选行动的差距不超过底池的 3%～5% 时，备选才有资格进入混合。
3. 使用低温 Softmax 或归一化权重选择，不使用固定“突然加注”开关。
4. 不确定性越高、对手越难预测，温度略升；高风险和多人底池温度下降。
5. 每个决策使用独立随机流，避免一个随机数同时控制持续下注、慢打和诈唬。
6. 每手牌使用可复现种子：`tableSeed + handId + seatId + decisionIndex`。
7. 通过频率记忆防止在同类局面长期只使用一个尺度。

可接受的变化来源：

- 对手行为后验的不确定性抽样。
- 近似等价行动的混合。
- 自身桌面形象和线路平衡。
- 不同 AI 的风险偏好和适应速度。

不可接受的变化来源：

- 无视牌力和赔率的随机全下。
- 明显负 EV 的“性格表演”。
- 为了让玩家赢而读取玩家底牌或篡改牌库。

## 12. AI 性格

所有 AI 都遵守同一套合理决策框架。性格通过连续参数表达，而不是特殊作弊规则。

建议参数：

```js
{
  riskAversion,
  aggression,
  bluffPreference,
  trapPreference,
  thinValuePreference,
  adaptationSpeed,
  uncertaintyTolerance,
  imageAwareness,
  patience
}
```

建议保留四类主要思路：

- 稳健型：范围较紧、风险惩罚较高、重视位置和控池。
- 压迫型：攻击封顶范围、擅长持续施压，但多人底池会收敛。
- 诱导型：更善于抓诈、慢打和过牌加注。
- 适应型：初始中性，对玩家变化的学习速度最快。

可以保留松凶或跟注型外观，但应改为“有意识接受高波动”或“根据赔率扩大防守”，而不是故意进行错误跟注。

## 13. 各阶段具体策略

### 13.1 翻牌前

- 使用位置、有效筹码和前方行动构建基础范围。
- 开池、跟注、3-bet 和 4-bet 使用不同组合范围。
- 3-bet 诈唬优先选择阻断牌和具有翻后可玩性的组合。
- 单挑和多人场景使用不同阈值。
- 12BB 以下切换为经过位置修正的推/弃与再推策略。
- 避免用固定牌力分数直接比较底池赔率；牌力必须相对于对手范围。

### 13.2 翻牌

- 判断范围优势、坚果优势、牌面湿润度和谁拥有位置。
- 小尺度持续下注用于整体范围优势，不等于“牌弱下注小”。
- 中大尺度用于极化、保护或强听牌半诈唬。
- 多人底池显著减少纯空气持续下注。
- 被下注时区分价值跟注、抓诈、听牌赔率和未来街可实现率。

### 13.3 转牌

- 重新判断新牌对双方范围的影响。
- 只有压力牌、权益改善或对手范围封顶时才提高二次开火频率。
- 对跟注站减少纯诈唬，增加价值尺度。
- 对过度弃牌者使用有阻断牌的延迟持续下注。
- 计划中记录哪些河牌继续、哪些河牌放弃。

### 13.4 河牌

- 没有未来权益，必须明确区分价值、薄价值、抓诈和纯诈唬。
- 诈唬优先选择阻断对手跟注范围、同时不阻断对手弃牌范围的组合。
- 价值下注尺度根据对手可跟注范围和自身形象调整。
- 面对下注时比较对手价值/诈唬组合，而不是只使用裸胜率。
- 大额河牌下注必须有可信的极化范围，禁止无理由大注。

## 14. 标准德州与短牌

共享决策流程，但规则参数必须由 `variant` 注入：

- 牌库和合法组合数量。
- A-6-7-8-9 短牌顺子处理。
- 同花与葫芦的牌型顺序。
- 更高的平均成牌强度和更低的边缘牌安全性。
- 短牌听牌权益、翻前范围和推/弃阈值。

不能通过复制完整策略文件维护两套 AI；差异必须集中在规则与参数模块。

## 15. 本地与联网完全统一

### 15.1 共享内容

- 同一个策略核心。
- 同一个性格配置。
- 同一个范围更新算法。
- 同一个胜率和回应模型。
- 同一个 HandPlan。
- 同一个混合策略和随机数算法。
- 同一套测试向量。

### 15.2 允许不同的部分

- 本地适配器从 `PokerGame` / `ShortDeckGame` 读取状态。
- 服务端适配器从 `ServerPokerGame` 读取状态。
- 本地动作由本地规则引擎执行；联网动作必须由服务端权威执行。
- 联网重连后，服务端负责保存或重建 AI 画像与计划。

### 15.3 一致性测试

建立 JSON 场景夹具。同一场景同时交给本地适配器和服务端适配器，断言：

- 生成的 `DecisionContext` 完全一致。
- 同一种子下返回相同行动与金额。
- 对手范围和画像更新结果一致。
- 标准德州和短牌分别通过。

## 16. 决策日志

每次行动生成仅用于开发和牌后分析的 `DecisionTrace`：

```js
{
  intent: 'pot_control',
  equity: { mean:0.41, confidence:0.78 },
  potOdds: 0.29,
  opponentRead: '范围偏强，但河牌诈唬频率略高',
  selfImage: '最近偏紧，当前跟注不会明显暴露范围',
  candidates: [
    { action:'fold', utility:0 },
    { action:'call', utility:34, risk:18 },
    { action:'raise', utility:-22, risk:71 }
  ],
  chosen: 'call',
  nextPlan: '安全转牌继续控池；大额二次开火重新评估'
}
```

实战中不能实时展示全部推理给玩家，否则会泄露 AI 策略。可以在开发面板或牌局结束后查看。

## 17. 性能预算

- 翻前决策目标：普通桌面设备低于 10ms。
- 翻后常规决策目标：低于 40ms。
- 复杂河牌和多人底池：尽量低于 80ms。
- 对相同公开状态、范围版本和候选尺度缓存胜率结果。
- 先进行一次共享蒙特卡洛，再复用到所有候选行动。
- 模拟次数根据决策接近程度自适应：差距明显时提前停止，接近时增加样本。
- 服务端多房间运行时设置总计算预算，不能阻塞 WebSocket 状态广播。

## 18. 测试与评估

### 18.1 单元测试

- 所有返回行动合法且金额在边界内。
- 底池赔率临界点的跟注/弃牌正确。
- 对紧范围的胜率低于对宽范围的胜率。
- 多人底池诈唬频率低于单挑。
- 对跟注站增加价值、减少空气诈唬。
- 对过度弃牌者增加可信诈唬。
- 被抓诈后自身形象正确改变。
- HandPlan 能延续，也能被新证据推翻。
- 同一种子结果可复现。
- 本地和服务端一致。

### 18.2 场景测试

建立至少 100 个固定场景：

- 不同位置和筹码深度的翻前开池、3-bet、4-bet。
- 单挑和多人湿润/干燥牌面。
- 强牌保护、薄价值、坚果听牌、弱听牌和抓诈。
- 对紧手、松手、跟注站、过度激进玩家。
- 河牌阻断牌诈唬和无阻断牌放弃。
- 短牌特殊顺子、同花与葫芦场景。

### 18.3 批量自对局

使用固定种子运行大量牌局，比较旧 AI、各新性格和简单基准策略：

- 每百手盈利 BB/100。
- VPIP、PFR、3-bet、持续下注和河牌下注频率。
- 价值下注与诈唬比例。
- 多人底池诈唬成功率。
- 不同筹码深度的破产率。
- 各下注尺度使用比例。
- 决策耗时 P50/P95/P99。
- 是否出现某一动作长期接近 100% 的僵硬模式。

不能只看 AI 是否赢得更多；还要检查玩法是否自然、可理解、具有不同风格且不存在明显可重复利用的漏洞。

## 19. 实施阶段

### 阶段一：统一基础设施

- 建立共享 `ai-core`。
- 定义 `DecisionContext`、`Decision` 和确定性 RNG。
- 编写本地/服务端适配器和一致性测试。
- 保持旧策略结果基本不变，先解决双实现问题。

预期效果：联网与本地 AI 完全一致，后续只维护一套代码。

### 阶段二：真实范围与胜率

- 实现组合权重范围。
- 公开行动更新范围。
- 范围加权蒙特卡洛。
- 行为画像加入置信度。

预期效果：AI 会真正尊重紧手的强线路，也会更准确攻击宽范围。

### 阶段三：回应树与风险效用

- 建立分尺度回应模型。
- 改进多人底池和反加注分支。
- 加入权益实现率、反向隐含赔率和风险预算。

预期效果：边缘局面更稳健，价值下注和半诈唬尺度更合理，减少无意义大底池。

### 阶段四：反向博弈与 HandPlan

- 建立 SelfImage 和线路可信度。
- 实现跨街计划和停止条件。
- 用计划指导持续下注、二次开火、诱导和河牌行动。

预期效果：AI 的行动前后连贯，能够根据对手如何理解自己来调整。

### 阶段五：有界混合与调优

- 以低温 Softmax 替换零散随机开关。
- 加入频率记忆和自对局统计。
- 调整不同性格参数。

预期效果：同类局面不再机械重复，但不会因为随机性做明显错误动作。

## 20. 其他值得同时修改的地方

1. **牌局行动历史标准化**：当前两端记录的数据结构不同，应统一为可重放事件流。
2. **服务端保存 AI 状态**：房间重连、玩家进出和下一手开始时，画像不能错误清空或串房。
3. **AI 状态版本号**：策略结构升级后为序列化状态加版本，避免旧存档不兼容。
4. **统一下注语义**：所有层明确区分 `raiseTo`、`additionalCost` 和 `callCost`。
5. **决策超时降级**：计算超过预算时使用经过测试的快速策略，不应卡住房间。
6. **隐私边界测试**：自动断言 AI 输入中不包含其他玩家未公开底牌。
7. **参数集中管理**：避免阈值散落在大量 `if` 中，所有参数应有名称、范围和说明。
8. **牌后可解释性**：开发模式支持导出单手决策日志，便于复现“不合理出手”。
9. **版本化回放**：保存随机种子、策略版本和行动事件，使问题牌局能够完全重播。
10. **音乐与思考节奏解耦**：背景音乐强度不能改变 AI 实际决策时间或随机种子。

## 21. 验收标准

完成后至少满足：

- 本地与联网同场景、同种子决策一致率 100%。
- 规则合法性测试无失败，筹码守恒无失败。
- AI 不使用任何未公开底牌信息。
- 明显负 EV 行动不会仅因随机性被选中。
- 近似等价行动有稳定但非固定的混合比例。
- 能针对紧手、松手、跟注站和激进玩家做出方向正确的调整。
- 同一手牌的翻牌、转牌、河牌行动能够从日志中看出连续计划。
- 多人底池纯诈唬明显少于单挑。
- 标准德州与短牌共享核心，不复制整套策略。
- 服务端多房间运行时决策耗时在预算内。

## 22. 推荐默认方向

默认 AI 应定位为“中上水平、可适应、重视风险但不胆小”。所有性格具备合格的赔率和范围意识；区别主要来自风险偏好、进攻时机、诱导倾向和学习速度，而不是是否故意犯低级错误。

开发时应先完成阶段一和阶段二，再决定反向博弈参数。这样可以先确保统一、合法、可测试，再逐步增加复杂度，避免一次性重写后难以判断行为变化来自哪里。

## 23. v1.3 实施记录（2026-08-23）

本设计已按以下方式落地：

- 新增环境无关的共享决策核心 `js/ai-core.js`，本地标准德州、本地短牌和联网房间均通过 `AIPlayer` 适配器调用同一实现；旧算法只保留为核心加载失败时的回退。
- 实现了带组合权重的对手范围、公开行动更新、行为后验与置信度、范围加权蒙特卡洛、牌面结构分析、权益实现率、风险惩罚和分尺度对手回应估计。
- 实现 `SelfImage`、线路可信度、跨街 `HandPlan`、停止条件、回应加注计划，以及只在近似等价候选间生效的低温有界混合。
- 六种 AI 性格共享合格的数学与规则基础，只调整风险厌恶、进攻、诈唬、薄价值、诱导、耐心和适应速度；标准德州与短牌共享策略结构。
- 公开行动会同时更新本地与服务端画像；摊牌时公开的牌型、失败诈唬和慢打强牌会继续更新后验。AI 输入仍不包含任何未公开底牌。
- 每个决策都生成 `unified-v1` 跟踪信息，包括范围摘要、胜率与方差、底池赔率、候选行动的 EV/风险/效用、最终选择和下一街计划。
- 同步计算默认预算为普通节点约 80 ms，河牌或五人以上复杂节点约 150 ms，核心硬上限 180 ms；达到预算后返回已完成样本的结果，不等待目标样本数。
- 新增共享策略一致性、紧/宽范围方向、跟注站/过度弃牌者反应、有界混合、六人性能与服务端适配测试。连同原有规则、边池、短全下、断线重连和房间同步测试，共 24 项自动测试通过。

### 后续可继续加强但不阻塞本版的项目

1. 把蒙特卡洛放入浏览器 Worker 后台线程，将可用计算时间提高到 0.4～1.2 秒，同时完全消除主线程顿挫风险。
2. 建立 100 个以上固定策略场景和长期自对局统计，按 VPIP、PFR、3-bet、诈唬价值比和 BB/100 做参数校准；当前测试重点是规则、方向正确性、一致性和性能边界。
3. 增加版本化单手回放文件，把公开事件流、随机种子和决策跟踪完整导出，便于复现极少数边缘出手。
4. 服务端多房间规模扩大后增加全局计算配额和分房间调度；当前单机房间已有单决策硬截止，不会因追求样本数无限阻塞。

## 24. v1.4 实施补充（2026-08-24）

- 本地标准德州和短牌 AI 已通过 `ai-worker.js` 在后台线程执行统一策略；服务端仍调用相同 `ai-core.js`，相同公开状态、性格和种子保持一致。后台超时或不可用时自动回退到同步共享核心，规则层仍会再次校验动作。
- AI 样本预算继续受核心硬截止保护。当前 4 人、24 手牌压力测试中，单次决策平均约 8 ms，最慢低于 50 ms；后台线程让较复杂节点也不会阻塞发牌和筹码动画。
- 胜率分析改为独立 Worker，修复翻前忽略真实对手人数和随机排序洗牌的问题；转牌/河牌单挑使用精确枚举，其余场景使用无偏蒙特卡洛与限时缓存。
- 标准德州、短牌和服务端共享决策编号、行动金额语义、边池拆分和未跟注筹码退还。`callCost`、`additionalPaid` 与 `raiseTo` 不再混用。
- 联网服务增加全局单 AI 计算队列、默认 3 房间/24 连接/单 IP 20 连接、消息频率限制、来源校验与 16 KiB 消息上限；单 IP 额度可容纳完整 10 人房并给重连留出余量，适配通常 1 个房间、峰值 2–3 个房间且少于 20 人的使用规模。
- 客户端断线恢复改为带抖动的指数退避，避免网络波动时所有客户端同时反复连接；重连成功后继续使用原座位、手牌和权威状态版本。
- 自动测试扩展到 31 项，覆盖概率核心、策略一致性、边池/全下、跨局清理、断线续连、房主迁移、房间上限、消息节流和来源限制。浏览器实测确认 AI Worker、胜率 Worker、手牌翻面和公共牌动画同时工作且无控制台错误。
